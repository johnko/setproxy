#!/bin/sh

cat /usr/local/etc/pkg.conf.sample >/usr/local/etc/pkg.conf

for i in .cshrc .profile ; do
    cat /root/${i} | grep -v 'HTTP_PROXY' >/root/${i}.tmp
    cat /root/${i}.tmp >/root/${i}
    rm  /root/${i}.tmp
done
